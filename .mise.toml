[tools]
bun = "latest"
python = "3.12"

[tasks.codex]
env = { YOLOBOX_FLAGS = "--gh-auth --host-auth" }

[tasks.claude]
env = { YOLOBOX_FLAGS = "--gh-auth" }

[tasks.gemini]
env = { YOLOBOX_FLAGS = "--gh-auth" }

[tasks.copilot]
env = { YOLOBOX_FLAGS = "--gh-auth" }

[tasks.opencode]
env = { YOLOBOX_FLAGS = "--gh-auth" }
